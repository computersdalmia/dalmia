# JANU Packs Rules

- `*.sh` must be valid bash.
- If Python is needed, embed it via: `python3 <<'PY' ... PY`
- Never paste HTML tags (`<script>`, `<link>`, `<li>`, etc.) into `.sh`.
- Generators should be `v3`. Older versions (`v2`) must be thin wrappers for backward compatibility.
